name: brew pr-pull

on:
  pull_request_target:
    types:
      - labeled

jobs:
  pr-pull:
    if: contains(github.event.pull_request.labels.*.name, 'pr-pull')
    runs-on: ubuntu-22.04
    permissions:
      actions: read
      checks: read
      contents: write
      issues: read
      pull-requests: write
      packages: write
    steps:
      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up git
        uses: Homebrew/actions/git-user-config@main

      - name: Pull bottles
        env:
          HOMEBREW_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PULL_REQUEST: ${{ github.event.pull_request.number }}
        run: brew pr-pull --debug --tap="$GITHUB_REPOSITORY" "$PULL_REQUEST"

      - name: Move commit to PR branch
        env:
          BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          # 1. Get the commit hash generated by brew pr-pull
          BOTTLE_COMMIT=$(git rev-parse HEAD)
          
          # 2. Checkout your PR branch
          git fetch origin $BRANCH
          git checkout $BRANCH
          
          # 3. Apply the changes from that commit (the bottle hashes)
          git cherry-pick $BOTTLE_COMMIT

      - name: Push commits
        uses: Homebrew/actions/git-try-push@main
        with:
          branch: ${{ github.event.pull_request.head.ref }}

      # - name: Delete branch
      #   if: github.event.pull_request.head.repo.fork == false
      #   env:
      #     BRANCH: ${{ github.event.pull_request.head.ref }}
      #   run: git push --delete origin "$BRANCH"
